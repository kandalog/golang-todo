version: "3.8"
services:
  api:
    build:
      context: ./api
      dockerfile: ./Dockerfile
    env_file: ./api/.env
    tty: true
    # depends_on:
    #   - db
    volumes:
      - ./api:/usr/src/app
    ports:
      - "8080:8080"
    environment:
      - TZ=Asia/Tokyo
