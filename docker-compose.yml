version: "3"
services:
  web:
    build:
      context: .
      dockerfile: ./backend/Dockerfile
    container_name: "web"
    tty: true
    volumes:
      - ./backend:/app
    ports:
      - "8080:8080"
